machine:
    php:
        version: 7.0.4
    services:
        - docker

dependencies:
    cache_directories:
        - "~/docker"
    pre:
        - docker build -t symm/hello:$CIRCLE_SHA1 .

test:
    post:
        - docker run symm/hello:$CIRCLE_SHA1 vendor/bin/behat
