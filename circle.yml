machine:
    php:
        version: 7.0.4
    services:
        - docker

dependencies:
    pre:
        - docker build -t symm/hello:$CIRCLE_SHA1 .

test:
    post:
        - docker run symm/hello vendor/bin/behat
